# Libs

This contains the data structures and functionality to implement a text buffer with a default implementation of an advanced Piece Table.

The second implementation is turning the Piece Table into a Piece Tree. This is essentially a Rust port of Microsoft's [TypeScript implementation]().
